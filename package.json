{
  "name": "sentinel-audit-agent",
  "version": "0.1.0",
  "private": true,
  "description": "Autonomous AI smart-contract audit agent for Mantle — on-chain ERC-8004 identity, pay-per-audit via x402.",
  "type": "module",
  "bin": {
    "sentinel": "./dist/cli/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx src/cli/index.ts",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@solidity-parser/parser": "^0.18.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
